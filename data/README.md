# Data

`dataset.csv` is the scored panel (features + label). Rebuild with:

```bash
python scripts/generate_sample.py
python scripts/train_model.py
```

The bundled sample plants a modest high-leverage to weaker next-21-session association so the offline demo has a learnable signal. That is a fixture, not a market result.
